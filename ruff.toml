# Applies to the hand-written Python services. packages/groovenet-python
# carries its own config and is generated, so it is excluded from the
# pre-commit hooks rather than linted here.
line-length = 120

[lint]
# Ruff's defaults (E4/E7/E9, F) plus import sorting and pyupgrade, matching
# what the generated client is already held to.
select = ["E4", "E7", "E9", "F", "I", "UP"]
